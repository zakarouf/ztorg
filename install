#!/bin/sh

CCF=gcc
CFLAGS='-Wall -Ofast'
HEADER='zse/ '
LDFLAGS='-lncurses'
ZENGINE='zse/*'

OUTEX_FILE='z'

tar -xf data.tar

# Get All the c files
read CFILES


$CCF $CFLAGS -std=$1 $LDFLAGS -I"$HEADER" -c $CFILES ztorg.c
$CCF $CFLAGS $LDFLAGS -o data/$OUTEX_FILE *.o

rm -rf *.o
