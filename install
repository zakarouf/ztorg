#!/bin/sh

CCF=gcc
CFLAGS='-Wall -Ofast'
LDFLAGS='-lncurses -lraylib -lm -lvulkan -Wl,-rpath -Wl,/usr/local/lib'
ZENGINE='zse/*'

OUTEX_FILE='z'

tar -xf data.tar

# Get All the c files
read CFILES


$CCF -std=$1 -c $CFILES
$CCF $LDFLAGS -o data/$OUTEX_FILE *.o

rm -rf *.o
